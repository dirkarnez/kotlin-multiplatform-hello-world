target 'iosApp' do
  use_frameworks!
  platform :ios, '14.1'
  pod 'shared', :path => '../shared'

    target 'iosAppTests' do
      inherit! :search_paths
      pod 'shared', :path => '../shared'
    end
end
